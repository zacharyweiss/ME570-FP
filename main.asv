% load world
load polygonWorld.mat

% settings
nVerts = 500;   % number of vertices to build tree to
deltaT = 0.1;   % stepsize of tree
iStart = 1;     % start position

% generate RRT
tree = rrt_generate(world,xStart(:,iStart),nVerts,deltaT);
% plot RRT
rrt_plot(world,tree)

% find preemptive best path at time of iteration cutoff
xPath = preempt_path(tree,xGoal);
%